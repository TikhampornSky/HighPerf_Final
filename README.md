# high perf project final

run `go build .`
run `./go-1 -input input/rand-1K.txt -output output/rand-1K.txt -profile cpu -profile-path ./profile -technique merge-concurrent`
# HighPerf_Final
# HighPerf_Final
